<!-- RagSettingsWindow.xaml -->
<Window x:Class="ollamidesk.RAG.Windows.RagSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RAG Configuration" Height="400" Width="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="RAG Configuration" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <GroupBox Header="Chunking Parameters" Margin="0,0,0,10">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Chunk Size:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                        <Slider Grid.Row="0" Grid.Column="1" Minimum="100" Maximum="1000" Value="{Binding ChunkSize}" 
                                TickFrequency="50" TickPlacement="BottomRight" Margin="10,0"/>
                        <TextBox Grid.Row="0" Grid.Column="2" Width="60" Text="{Binding ChunkSize}"/>

                        <TextBlock Text="Chunk Overlap:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                        <Slider Grid.Row="1" Grid.Column="1" Minimum="0" Maximum="200" Value="{Binding ChunkOverlap}" 
                                TickFrequency="10" TickPlacement="BottomRight" Margin="10,0"/>
                        <TextBox Grid.Row="1" Grid.Column="2" Width="60" Text="{Binding ChunkOverlap}"/>
                    </Grid>
                </GroupBox>

                <GroupBox Header="Retrieval Parameters" Margin="0,0,0,10">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Max Chunks:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                        <Slider Grid.Row="0" Grid.Column="1" Minimum="1" Maximum="10" Value="{Binding MaxRetrievedChunks}" 
                                TickFrequency="1" TickPlacement="BottomRight" Margin="10,0"/>
                        <TextBox Grid.Row="0" Grid.Column="2" Width="60" Text="{Binding MaxRetrievedChunks}"/>

                        <TextBlock Text="Min Similarity:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                        <Slider Grid.Row="1" Grid.Column="1" Minimum="0" Maximum="1" Value="{Binding MinSimilarityScore}" 
                                TickFrequency="0.1" TickPlacement="BottomRight" Margin="10,0"/>
                        <TextBox Grid.Row="1" Grid.Column="2" Width="60" Text="{Binding MinSimilarityScore, StringFormat=N2}"/>
                    </Grid>
                </GroupBox>

                <GroupBox Header="Advanced Parameters" Margin="0,0,0,10">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Semantic Chunking:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                        <CheckBox Grid.Row="0" Grid.Column="1" IsChecked="{Binding UseSemanticChunking}"/>
                    </Grid>
                </GroupBox>

                <TextBlock TextWrapping="Wrap" Margin="0,10">
                    Note: Changes to these settings will affect future document processing but won't 
                    automatically reprocess existing documents.
                </TextBlock>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Reset Defaults" Command="{Binding ResetCommand}" Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="Save" Command="{Binding SaveCommand}" Padding="20,5" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Padding="10,5"/>
        </StackPanel>
    </Grid>
</Window>